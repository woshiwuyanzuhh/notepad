# 记事本 · 手动冒烟测试清单

> 每次发布前按顺序走查；每项记录 通过/失败 + 备注。

## 环境
- [ ] 测试日期 / 版本号：________

## 一、安装与首启
- [ ] 1.1 NSIS 安装包能正常安装（默认路径），桌面/开始菜单出现「记事本」
- [ ] 1.2 首次启动显示欢迎页（渐变图标 + 选择笔记文件夹按钮）
- [ ] 1.3 选择文件夹（新建空文件夹）后：自动创建欢迎笔记并打开
- [ ] 1.4 重启应用：直接进入主界面（不再显示欢迎页），笔记列表出现欢迎笔记
- [ ] 1.5 数据目录验证：所选文件夹中出现 `欢迎使用记事本.md` 和 `.notebook-meta.json`

## 二、笔记基础操作
- [ ] 2.1 新建笔记（标题栏 ＋ / Ctrl+N / 标签栏 ＋）：立即打开新标签
- [ ] 2.2 输入内容 → 停止 1 秒 → 状态栏变「已保存」（绿点），文件已写入磁盘
- [ ] 2.3 多标签：打开 2+ 笔记，切换、关闭（× / Ctrl+W / 中键），脏点 ● 显示
- [ ] 2.4 标题带非法字符（`a/b\c:d`）新建：文件名被清理，不报错
- [ ] 2.5 同名新建：自动加 `(2)` 后缀
- [ ] 2.6 保存状态：编辑中显示「保存中…」（黄点），保存后「已保存」（绿点）

## 三、编辑与预览
- [ ] 3.1 格式工具条：加粗/斜体/H1/H2/列表/引用/行内代码/链接/图片 在光标处插入正确语法
- [ ] 3.2 编辑/预览/分栏三种模式切换正常，分栏左右同步滚动（如无则记录）
- [ ] 3.3 预览：标题、列表、表格、引用、任务列表 `- [x]` 渲染正确
- [ ] 3.4 代码块：```python 等 5 种语言高亮，语言徽章显示，复制按钮可用
- [ ] 3.5 中文输入法：正文输入流畅无吞字

## 四、JSON 工具
- [ ] 4.1 笔记内容为 JSON（或含 ```json 块）→ 出现「JSON 工具」条
- [ ] 4.2 格式化 JSON：缩进美化且编辑器内容被替换
- [ ] 4.3 校验：合法显示「✓ JSON 有效」；非法显示行/列错误
- [ ] 4.4 树状查看：展开/收起、全部展开/收起、键值配色（键蓝/字符串绿/数字橙/布尔紫红）

## 五、整理收纳
- [ ] 5.1 文件夹过滤：点侧栏文件夹 → 列表只显示该文件夹笔记；新建笔记进入当前文件夹
- [ ] 5.2 标签：星标/置顶 卡片悬停操作生效，置顶笔记排最前
- [ ] 5.3 回收站：删除笔记 → 回收站计数 +1；恢复 → 回到原文件夹；彻底删除 → 文件消失
- [ ] 5.4 侧栏折叠（窄条图标模式）与展开

## 六、搜索
- [ ] 6.1 顶部搜索：中文/英文关键词过滤笔记（标题/正文/标签）
- [ ] 6.2 命中高亮 `<mark>` 显示；无结果显示「未找到与…相关的内容」
- [ ] 6.3 清空搜索恢复全部

## 七、主题与外观
- [ ] 7.1 标题栏月亮/太阳按钮切换深浅主题，全局配色即时变化
- [ ] 7.2 设置弹窗内「浅色/深色」分段控件同步生效
- [ ] 7.3 重启后主题保持（localStorage 持久化）
- [ ] 7.4 无边框窗口：拖拽标题栏移动窗口、双击最大化、右上角 最小化/最大化/关闭 按钮可用（Windows 风格，关闭 hover 变红）
- [ ] 7.5 纯净编辑模式：标题栏展开图标进入（侧栏/列表隐藏，编辑区全宽居中），Esc 或图标退出

## 八、健壮性
- [ ] 8.1 外部修改冲突：用记事本打开某笔记 → 用其他编辑器修改该文件 → 回记事本编辑保存 → 弹「文件冲突」→ 选「覆盖」或「重新加载」均正确
- [ ] 8.2 快捷键：Ctrl+N 新建、Ctrl+S 保存、Ctrl+W 关闭、Ctrl+Tab 切换
- [ ] 8.3 大量笔记（100+）列表滚动流畅
- [ ] 8.4 深色主题下代码块/JSON 树可读性正常

## 九、卸载
- [ ] 9.1 控制面板卸载「记事本」干净（数据目录保留，符合预期）
- [ ] 10.1 新建格式：新建按钮旁下拉可选 Markdown（.md）/ 文本（.txt），文件扩展名正确
- [ ] 10.2 图片插入：点图片按钮 → 选本地图 → 自动复制到数据目录 assets/ 并插入 ![](assets/..)，预览立即显示
- [ ] 10.3 预览模式点格式按钮：自动切回编辑模式并插入语法
- [ ] 10.4 字体：设置 → 编辑器字体（列出本机字体）与字号，txt 编辑即时生效并持久化
- [ ] 10.5 txt 自动换行：状态栏开关默认关（长行水平滚动），打开后自动换行，仅 txt 显示
- [ ] 10.6 LaTeX：md 笔记中 $x^2$ 行内公式与 $$\frac{1}{2} 块级公式渲染为 KaTeX
